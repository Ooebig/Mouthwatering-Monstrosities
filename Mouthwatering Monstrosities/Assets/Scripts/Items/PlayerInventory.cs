using NUnit.Framework;
using UnityEngine;
using UnityEngine.UIElements;
using System.Collections.Generic;

public class PlayerInventory : MonoBehaviour
{
    public List<InventoryItem> playerInv = new List<InventoryItem>();
    private Dictionary<ItemDrops, InventoryItem> itemDictionary = new Dictionary<ItemDrops, InventoryItem>();

    public void Add(ItemDrops item) { 
    if(itemDictionary.TryGetValue)
    
    }
}
